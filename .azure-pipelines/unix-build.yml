# Script for building CCTBX on linux and macOS
#
# Variables:
#   PYTHON_VERSION: py27, py36
#   OS: linux-64, osx-64

steps:

- checkout: self
  path: ./modules/dials

- script: |
    echo I am here:
    pwd
    echo This is what lives here:
    ls -la
    ls -la *
  displayName: Some basic scripting
  workingDirectory: $(Pipeline.Workspace)

- script: |
    python modules/dials/installer/bootstrap.py update
  displayName: Update sources
  workingDirectory: $(Pipeline.Workspace)

- script: |
    echo I am here:
    pwd
    echo This is what lives here:
    ls -la
    ls -la *
  displayName: Some basic scripting
  workingDirectory: $(Pipeline.Workspace)

- script: |
    python modules/dials/installer/bootstrap.py base
  displayName: Installing base environment
  workingDirectory: $(Pipeline.Workspace)

- script: |
    echo I am here:
    pwd
    echo This is what lives here:
    ls -la
    ls -la *
  displayName: Some basic scripting
  workingDirectory: $(Pipeline.Workspace)

- script: |
    python modules/dials/installer/bootstrap.py build
  displayName: Build
  workingDirectory: $(Pipeline.Workspace)

- script: |
    echo I am here:
    pwd
    echo This is what lives here:
    ls -la
    ls -la *
  displayName: Some basic scripting
  workingDirectory: $(Pipeline.Workspace)

- script: |
    python modules/dials/installer/bootstrap.py test
  displayName: Run tests
  workingDirectory: $(Pipeline.Workspace)

- script: |
    echo I am here:
    pwd
    echo This is what lives here:
    ls -la
    ls -la *
  displayName: Some basic scripting
  workingDirectory: $(Pipeline.Workspace)
